<h1 class="current-category"><%= params[:category] %></h1>

<% if @plays.count == 0 %>
	<h3>No Plays In Current Category</h3>
<% else %>
  <% @plays.each do |play| %>
    <h3><%= link_to play.title, play_path(play) %></h3>
  <% end %>
<% end %>
